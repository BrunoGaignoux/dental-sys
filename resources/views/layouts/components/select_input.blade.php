<div class="{{ $col }}">
    <div class="form-group">
        <label for="{{ $labelName }}">{{ $name }}</label>
        <select id="{{ $selectId }}" name="{{ $selectName }}" class="form-control">
            {{ $options }}
        </select>
    </div><!-- /.form-group -->
</div><!-- /.col -->